See README.md's "Quickstart" section for the full, current setup steps
(Docker Compose, or running each piece directly) — kept there, not
duplicated here, so this file can't drift out of sync with it again.

Fastest path:
    cp backend/.env.example backend/.env
    # set JWT_SECRET_KEY in backend/.env
    docker compose up -d --build
    curl http://127.0.0.1:5000/healthz
